/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        qualities: [75, 100], // разрешени стойности за quality
        domains: ["i.imgur.com"],
      }
};

export default nextConfig;
