export const metadata = {
    title: 'Gallery | STASI STIL HAIR SALON'
  };

  
export default function GalleryLayout({ children }){
    return (
        <>
          {children}
        </>
      );
}